217. 存在重复元素

给定一个整数数组，判断是否存在重复元素。
如果存在一值在数组中出现至少两次，函数返回 true 。如果数组中每个元素都不相同，则返回 false 。

**
学会了使用else: continue; break跳出多层循环

**
第一次使用直接用第一个数，往后找重复；然后第二个数。。。 结果超出时间限制

**
先nums.sort()，再测试nums[i]和nums[i+1]是否一样，能大量减少时间

**
使用list.set(),把重复的整合，再看length是否一致
